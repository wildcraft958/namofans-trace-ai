"""Run the end-to-end demo flow against a running API server.

Walks through: ingest seed → wait for alerts → fetch explanation → download STR → trigger drift.
"""

from __future__ import annotations


def main() -> None:
    print("[scaffold] would walk demo scenario through the live API")


if __name__ == "__main__":
    main()
