/**
 * DriftTimeline — shows ADWIN drift events over time. Used for the live retraining demo.
 */
export default function DriftTimeline() {
  return (
    <div style={{ background: "#11141d", borderRadius: 8, padding: 12 }}>
      <h4 style={{ margin: 0 }}>Concept Drift</h4>
      {/* TODO(frontend): fetch drift events and plot a sparkline with red bars on drift */}
    </div>
  );
}
